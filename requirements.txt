# PLACEHOLDER — populated in A2 (T0.1). Not installable yet.
#
# A2 rules (docs/08-coding-workflow.md, docs/06-devops-guide.md §2.2):
#   - Every line must be PINNED (==). An unpinned transitive dependency defeats the
#     SHA-tagged deployment model.
#   - pip-compile / uv hash-pinning is preferred, not mandated.
#
# Three versions are an open T0.1 decision and must NOT be guessed:
#   - Python  (python:3.12-slim in DevOps §12 is labelled "a concrete example";
#              GeoDjango support must be confirmed before pinning)
#   - Django
#   - DRF
#
# Package set (all named in docs 02/05/06/07):
#   django, djangorestframework, djangorestframework-gis, psycopg, django-environ,
#   celery, redis, django-storages, boto3, Pillow, django-otp, argon2-cffi, uvicorn,
#   structlog, django-prometheus, opentelemetry (Django + Celery instrumentation),
#   pytest, pytest-django, factory_boy, ruff, mypy
